#!/bin/bash
# A script to preprocess a C file and save the result to another file

# Check if the CFILE variable is set
if [ -z "$CFILE" ]; then
    echo "Error: CFILE environment variable is not set."
    exit 1
fi

# Run the preprocessor and save the output to 'c'
gcc -E "$CFILE" -o c

